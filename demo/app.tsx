// import { useCombinedGetterSetter } from '../src';

export function App() {
	// const state = useCombinedGetterSetter(1);

	return (
		<>
			<p>Hello Vite + Preact!</p>
			<p>
				<a
					href="https://preactjs.com/"
					target="_blank"
					rel="noopener noreferrer"
				>
					Learn Preact
				</a>
			</p>
			{/* <button onClick={() => state(state() + 1)}>Click {state()}</button> */}
		</>
	);
}
